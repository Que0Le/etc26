info:
  name: Get all devs
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/developers
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
